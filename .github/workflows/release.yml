name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.1'
          tools: composer:v2

      - name: Install dependencies
        run: composer install --no-dev --optimize-autoloader

      - name: Create release ZIP
        run: |
          mkdir -p build
          zip -r build/lw-seo.zip . \
            -x ".git/*" \
            -x ".github/*" \
            -x "build/*" \
            -x ".gitignore" \
            -x "composer.lock" \
            -x "phpcs.xml.dist" \
            -x "phpunit.xml.dist" \
            -x "tests/*"

      - name: Upload Release Asset
        uses: softprops/action-gh-release@v1
        with:
          files: build/lw-seo.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
